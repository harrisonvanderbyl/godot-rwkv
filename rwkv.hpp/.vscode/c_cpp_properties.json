{
    "configurations": [
        {
            "name": "Linux",
            "includePath": [
                "${workspaceFolder}/**",
                "${workspaceFolder}/include",
                // vulkan
                "/usr/include/vulkan",
            ],
            "defines": [],
            // intel icpx
            "compilerPath": "/opt/intel/oneapi/2024.0/bin/icpx",
            "cStandard": "c17",
            "cppStandard": "c++20",
            "intelliSenseMode": "intel-icpx",
            // -march=native  -O3 -std=c++17 -fopenmp  -flto  -fopenmp -funroll-loops -D_GLIBCXX_PARALLEL
            "compilerArgs": [
                // avx512 and bf16 (assuming current CPU supports it)
                "-march=native",
                // avx512 but no bf16
                // "-march=skylake-avx512",
                // avx2 (no accumulation?)
                // "-march=haswell",
                // arm
                // "-march=aarch64",
                "-O3",
                "-std=c++17",
                // vulkan

            ]
        }
    ],
    "version": 4
}